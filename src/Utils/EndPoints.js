import BASE_URL from "../../config";

export const EndPoints = {
  searchProducts: `${BASE_URL}/get/search-product`,
  getProductOffer: `${BASE_URL}/get/get-product-offers`,
  graphImageWithProxy: `${BASE_URL}/get/get-graph-image`,
};
